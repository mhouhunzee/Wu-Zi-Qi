# PyTorch - 使用CPU版本避免CUDA兼容性问题
# 如需GPU支持，请访问 https://pytorch.org/get-started/locally/ 安装对应版本
--index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# 其他依赖
numpy>=1.24.0
flask>=2.3.0
flask-cors>=4.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
